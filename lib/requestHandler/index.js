/**
 * routeHandler defines functions of the form function(req) for every direct
 * transaction with api v2. routeHandler functions only deal with logic.
 *
 * routeHandler functions return an object of the type:
 *
 * {
 * 		body: Object
 * 		
 * }
 * 
 */

module.exports = {
	// getInvoice: require('./getInvoice'),
	// submitInvoice: require('./submitInvoice'),
	// getOwnQueues: require('./getOwnQueues'),
	// getBusinessProperties: require('./getBusinessProperties'),
};